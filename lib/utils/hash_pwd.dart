//import 'package:password_hash/password_hash.dart';
//
//void  hashPassword(){
//  String password = "my_password";
//  final scrypt = Scrypt();
//  final hashedPassword = scrypt.hash(password, rounds: 8, memLimit: 14);
//  print(hashedPassword); // This will print the hashed password.
//}
